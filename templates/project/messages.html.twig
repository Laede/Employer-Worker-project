{% if messages %}
<table class="table">
    <tbody>
    {% for message in messages %}
        <tr>
            <td>{{ message.user.fullName }}</td>
            <td>{{ message.timestamp ? message.timestamp|date('Y-m-d H:i:s') : '' }}</td>
            <td>{{ message.message }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endif %}
{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success btn-sm">{{ button_label|default('Send') }}</button>
<br/>
<br/>
{{ form_end(form) }}